# Expo Go development needs no API variable: the app follows Metro's LAN host.
# Standalone local builds must provide a private address at build time, e.g.:
# EXPO_PUBLIC_API_URL=http://192.168.1.100:5000/api
