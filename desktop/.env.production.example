# ── TimeLogic Desktop (Admin, Electron) — PRODUCTION env ─────────────────────
# Local-first build values. The source currently enforces these endpoints.

VITE_API_URL=http://localhost:5000/api

VITE_SOCKET_URL=http://localhost:5000
