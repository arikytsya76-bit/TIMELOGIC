services:
  - type: web
    name: timelogic-backend
    runtime: node
    rootDir: backend
    buildCommand: npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
